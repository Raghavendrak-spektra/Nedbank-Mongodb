## MetaData
Question Type : Single Choice

## Question
The reporting application only needs to view customer data in the `customer360` database. Which role assignment best follows the least-privilege requirement for the `c360Reporter` user?

## Options
Option 1 : Assign `dbAdmin` on `customer360` so the reporting user can manage indexes and database metadata.

Option 2 : Assign `read` on `customer360` only, with no write or administrative roles.

Option 3 : Assign `readWrite` on `customer360` so reports can insert test records when needed.

Option 4 : Assign `root` on the `admin` database so the account can troubleshoot permission issues independently.

## Answers
Option 2 : 1

## Correct Answer Feedback
Option 2 is correct

## Number of Retries
1

